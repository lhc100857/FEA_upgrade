# PATCH 01: .mdc description frontmatter

**目标文件**: `.cursor/rules/hajimidog-4plus1-swarm.mdc`
**位置**: 文件头部 `---` 之间的 `description:` 行

## 查找（旧文本特征）

```
执行细则与路由表见技能 08/09/11/12/13/14/16。
```

## 替换为

在 `description:` 值中追加以下内容（在 `可重复脚本工具链` 或末尾分号后）：

```
、主帅上下文生命周期管理（三层模型/CHAIN-STATE/delta汇流/EP归档/pinned摘要/会话切割）
```

并将触发话术追加：

```
、CHAIN-STATE、delta、会话切割
```

并将技能引用改为：

```
执行细则与路由表见技能 08/09/11/12/13/14/16/22。
```
